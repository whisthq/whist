# no need to exec, we exec the openRTSPclient file

#set(EXEC hello_video.bin)
#set(SRCS video.c)

#add_executable(${EXEC} ${SRCS})
#target_link_libraries(${EXEC} ${HELLO_PI_LIBS})

#install(TARGETS video.c
#        RUNTIME DESTINATION bin)

