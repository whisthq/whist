{
  "environments": {
    "review": {
      "addons": ["heroku-redis:hobby-dev"]
    },
    "test": {
      "scripts": {
        "test-setup": "./ci_env_setup.sh; source test_setup.sh",
        "test": "py.test -v -r A",
        "addons": ["heroku-postgresql:in-dyno", "heroku-redis:in-dyno"]
      }
    }
  }
}
