{
  "environments": {
    "review": {
      "addons": ["heroku-redis:hobby-dev"]
    },
    "test": {
      "scripts": {
        "test-setup": "source ci_env_setup.sh; source test_setup.sh; cd tests;",
        "test": "py.test -v -s -o log_cli=true -x -m vm_serial && py.test -v -s -o log_cli=true -x -m disk_serial;"
      },
      "formation": {
        "web": {
          "quantity": 2,
          "size": "hobby"
        },
        "celery": {
          "quantity": 1,
          "size": "hobby"
        }
      }
    }
  }
}
