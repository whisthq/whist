{
  "environments": {
    "review": {
      "addons": ["heroku-redis:hobby-dev"]
    },
    "test": {
      "scripts": {
        "addons": ["heroku-postgresql:in-dyno", "heroku-redis:in-dyno"],
        "postdeploy": "parallelize.sh 'pytest -o log_cli=true -s -x -m disk_serial' 'pytest -o log_cli=true -s -x vm_serial'"
      }
    }
  }
}
