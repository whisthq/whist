{
  "scripts": {
    "postdeploy": "pytest --log-cli-level=10"
  },
  "environments": {
    "review": {
      "addons": ["heroku-redis:hobby-dev"]
    },
    "test": {
      "scripts": {
        "test": "pytest --log-cli-level=10",
        "addons": ["heroku-postgresql:in-dyno", "heroku-redis:in-dyno"]
      }
    }
  }
}
