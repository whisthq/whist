# These folders/files are generated by our install/build scripts and should not 
# be committed to git history
node_modules/
build/
src/config.json

# yarn errors will appear here during development, we don't need to track this
yarn-error.log